# Daily About-the-Author run contract — Post-AGI Planning

**This file is the authoritative operational contract for the scheduled About-the-Author workflow.**
The scheduler prompt is a short pointer to it.

Same reasoning as `DAILY-RUN.md`: a contract stored in a scheduler field is not version-controlled,
not diffable, not reviewable and not mirrored. Here it is all four, and a change to how this run
behaves leaves a trace.

Change it by editing this file in a reviewed change, exactly like any other gate.

---
Daily refresh of the “About the Author” section on The Hitchhiker's Guide to the Singularity / Post-AGI Planning site (peterxing.com + post-agi-planning.vercel.app). This workflow runs once daily. Its author-content job is to keep the bottom-of-page About section current with Peter Xing's LinkedIn profile and latest real talks. It also acts as a FAIL-CLOSED deployment guard: it must never redeploy a site whose prediction evidence has regressed below complete accounting — every prediction cited by a live-verified news article or explicitly recorded as having no qualifying source in the window. Work autonomously; never fabricate a talk, venue, date, URL, biography claim, publisher, byline, quote, provenance or evidence mapping.

FILES AND SAFETY
- Local source: C:\Users\peterxing\pap-deploy
- Public Vercel bundle: C:\Users\peterxing\pap-site
- Author source: C:\Users\peterxing\pap-deploy\author.json
- Forecast/evidence sources that this job must preserve: predictions.json, signals.json and evidence-floors.json
- Credentials/raw activity/private history/private archive corpus: C:\Users\peterxing\pap-secrets only. Never print, serve, copy to pap-site, or commit anything from there.
- .pipeline.lock is local coordination state only. Never serve, deploy, stage or commit it; server.js, deploy.ps1 and publish-github.ps1 all reject it and `npm run verify:interlock` proves that.
- The public deploy surface is an ALLOW-LIST, not a deny-list. .vercelignore excludes everything with `*` and re-includes only index.html, app.js, styles.css, signals.json, predictions.json, author.json, vercel.json and LICENSE, so any new script, corpus, cache, ledger or log is excluded BY DEFAULT instead of being published because nobody remembered to add a deny rule. Production is served by the Vercel Git integration from the GitHub mirror, so the repository copy of .vercelignore is the live gate. Never widen it to ship a new file; `npm run verify:surface` enforces the shape and `node verify-deploy-surface.js --live` proves it on both domains.
- Preserve the current Clawpilot theme, exact title “The Hitchhiker's Guide to the Singularity,” responsive design, figures/animations, accessibility, escaping/sanitization and reduced-motion behavior.

0) RUN CONTINUITY AND MISSED-RUN RECOVERY — DO THIS FIRST
ACQUIRE THE TREE INTERLOCK BEFORE ANY OTHER STEP. Scheduled runs and interactive sessions share the pap-deploy tree, and reading a half-applied tree is exactly the failure being prevented, so claim it before the first read of predictions.json, signals.json or evidence-floors.json and before any write. From C:\Users\peterxing\pap-deploy pick a unique owner id for this run (for example scheduled-author-yyyyMMddHHmm), export it in every subsequent command block as $env:PAP_PIPELINE_OWNER, and run:
  node pipeline-lock.js acquire --owner=$env:PAP_PIPELINE_OWNER --purpose=scheduled-author --wait=600
Exit 0 means the tree is yours. Exit 75 means DEFERRED: another live actor holds the tree, this run has read and written nothing, and it must stop immediately — publish nothing, change nothing, report DEFERRED plainly, and do NOT retry in a loop or force-break a live lock. A DEFERRED run counts as "did not publish", so the next scheduled run must treat it as a missed run under the CATCH-UP rule below. A stale lock (holder pid dead, or older than 90 minutes) is reclaimed automatically and reported loudly; when that happens, treat the interrupted previous run as a missed run too. Every guarded tool inherits this lock, so the whole run is protected. ALWAYS release at the very end, on success and on failure alike:
  node pipeline-lock.js release --owner=$env:PAP_PIPELINE_OWNER
Then establish whether the previous scheduled run completed and published. Compare today's date against author.json's updated date, signals.json's `updated`/sourceFetchedAt, evidence-floors.json, and the latest GitHub mirror commit. If the previous run failed, was skipped, errored during session creation, was DEFERRED by the interlock, or never published, treat today as an explicit CATCH-UP run, say so in the report, and run the full author and guard pipeline rather than assuming existing outputs are current.
LOCK LIVENESS IS PART OF CONTINUITY. At the start of every run, before any evidence work, read the interlock state and report it explicitly as one of: free; held (owner, purpose, lock age and HEARTBEAT AGE in minutes); or reclaimed-stale (with the reclaim reason). Print the heartbeat age for any lock you encounter or hold, and print your own final lock disposition — released, still held, or reclaimed — as the last line of the run. A run that ends without an explicit lock disposition line is an incomplete run and must be reported as such. If you reclaim a stale lock, treat the run that held it as a MISSED RUN under this section, absorb its work, and say so in the report. Never infer that a previous run succeeded merely because a lock file is absent or because a process is gone; confirm success against the published artifact timestamp on both live domains.
HEARTBEAT AGE IS NOT A LIVENESS SIGNAL, AND THE ORPHAN GRACE IS THINNER THAN IT LOOKS. pipeline-lock.js justifies its orphan rule on the premise that every guarded tool heartbeats on entry and then every HEARTBEAT_MS (60 seconds) while it works. Independent monitoring of a live, healthy run on 2026-08-11 sampled the lock every 45 seconds and measured heartbeat ages far beyond that cadence: repeated 5-7 minute gaps and a peak of 11.3 minutes before the next beat, meaning a live run routinely misses ten or more consecutive beats. Nothing was at risk on that run — STALE_MINUTES is 90 and the orphan path additionally requires a dead supervisorPid — but the worst observed gap is 75 percent of the 15-minute ORPHAN_MINUTES grace, leaving under 4 minutes of margin. Therefore: never treat a stale-looking heartbeat as evidence that another holder is dead. A held lock means DEFERRED exit 75 regardless of heartbeat age, and only pipeline-lock.js itself may decide a reclaim. Never lower PAP_LOCK_ORPHAN_MINUTES or PAP_LOCK_STALE_MINUTES, and treat any change that narrows the margin between the worst observed heartbeat gap and the orphan grace as a concurrency regression. Report the maximum heartbeat age observed during the run on the same line as the lock disposition, and if it exceeds 12 minutes flag it as an anomaly rather than passing over it. A false reclaim is strictly worse than a deferral: it declares a LIVE run dead, and the reclaim path then instructs the next run to treat that interrupted work as a missed run. UPDATE 2026-08-11: this was fixed in code rather than left as a rule, so do not read the 15-minute grace or the under-4-minutes margin above as current. ORPHAN_MINUTES now defaults to 30 minutes, putting 18.7 minutes of margin against the worst observed 11.3-minute silence, and PAP_LOCK_ORPHAN_MINUTES, PAP_LOCK_STALE_MINUTES and PAP_LOCK_HEARTBEAT_MS now ratchet one way only: an environment variable may make each gate stricter but can never weaken it, the same monotonic rule evidence-floors.json applies to the coverage gates. Correct the mechanism too, because the premise above understates it: the heartbeat does NOT beat every 60 seconds throughout a run. It advances when a guarded tool ENTERS, and then every HEARTBEAT_MS only for as long as that tool own process stays alive, so nothing beats at all between guarded tools and multi-minute silences are normal, healthy and expected rather than a symptom. Heartbeat age is an ACTIVITY signal, not a LIVENESS signal. verify-interlock.js now proves the grace stays at or above 30 minutes and that a narrowing environment variable is refused, so this is enforced by a gate rather than by memory.
A RUN STATUS IS NOT A LIVENESS SIGNAL EITHER, AND A STUCK ONE SILENTLY DISABLES THIS AUTOMATION. On 2026-08-09 a manual run of the predictions workflow was recorded status=running with completed_at NULL and never left that state, because its workspace row had been deleted while its session row survived: the run was orphaned, not slow. A non-terminal run suppresses BOTH the scheduled tick and any manual trigger, so nextRunAt passed 4.9 hours without firing and run_workflow refused with "already running". Nothing self-corrected and the workflow stayed dark for two days, while the author workflow on the SAME host and the SAME scheduler ran normally every day, so a healthy sibling workflow is not evidence that this one is running. Toggling enabled recomputes nextRunAt but does NOT clear latestRun, making it necessary and insufficient. Every run must therefore establish its own continuity from durable state only: a completed_at timestamp, the published artefacts fetched from BOTH live domains, and the coverage counters. Never infer it from a status field, from updated_at, or from any age-like field, because sessions.updated_at and get_session.updated_at both under-report activity by hours or days on runs that demonstrably completed, and heartbeat age measures activity rather than liveness. If a run finds its own previous run recorded as non-terminal with no completed_at, it must report that explicitly as a stalled-automation anomaly naming the stuck run ID and its start time, state how many scheduled ticks were missed, and proceed as a CATCH-UP. It must never mark such a run completed: completed launders a run that never happened into the history the next tick reads, and failed is the only honest terminal state for work that did not finish. READ THAT TRIGGER BROADLY, BECAUSE ITS NARROW FORM WOULD NOT HAVE FIRED ON THE INCIDENT THAT PRODUCED IT. Once the stuck run above was repaired it was recorded failed WITH a completed_at, so a run testing only for "non-terminal with no completed_at" would find the condition absent and reason its way back to NORMAL with this paragraph apparently blessing it. A predecessor recorded failed is a failed predecessor whether or not it carries a completed_at, and whether it was marked so by the scheduler, by a human, or by a repair: the terminal state records how the run ENDED, not whether its work was DONE. Treat non-terminal, failed, skipped, errored, DEFERRED and never-published as one class, and on any of them declare CATCH-UP explicitly. VERIFIED-CURRENT PRODUCTION DOES NOT LICENSE A NORMAL DECLARATION. It proves only that the LAST publish succeeded; it can never prove that NO TICK WAS MISSED, and those are different claims. Throughout the 2026-08-09 to 2026-08-11 outage both live domains served a complete, valid, internally consistent, fully-accounted build the entire time, so a run that accepted healthy artefacts as proof of a healthy schedule would have declared NORMAL on every single day the workflow was dark. The continuity test above is a CONJUNCTION and completed_at comes first: artefacts and coverage counters corroborate it, they never substitute for it. CATCH-UP is a statement about the predecessor RECORDED STATE, not a forecast of how much work will be found, so declare it even when the rebuild then finds nothing to absorb, and say so in that case: name the failed run and its start time, state how many ticks were missed, note that production was verified current on both domains, and confirm the full pipeline ran regardless.
INFRASTRUCTURE FAULTS ARE NOT EVIDENCE FAULTS. Retry transient infrastructure errors — session/network creation failure, DNS or socket error, HTTP 5xx, rate limiting, LinkedIn or publisher timeouts, a publisher bot-challenge/interstitial, Vercel/GitHub API blips — up to 3 times with exponential backoff (about 30s, 2m, 5m). Never convert a transient failure into an evidence or author change: do NOT drop a talk, drop a mapping, lower a floor, demote or expire a sticky approval, drop or re-review a currency reference, or weaken a gate because a network call failed. If retries are exhausted, STOP, change nothing, leave the published site exactly as it is, release the interlock, and report the precise failing step and error. Always distinguish infrastructure failure, interlock deferral and genuine verification failure in the report.
Emit and surface the safe sourceStatus reason and required human action from the evidence guard (for example: "news source unreachable — retry scheduled"). The X API is retired and must never be called; live-verified news retrieval must keep working without it, and a degraded API state must never by itself block publication or reduce Peter's represented voice. NEVER print, log, serve or commit raw auth responses, headers, tokens or credentials.

1) RESEARCH REAL, RECENT AUTHOR INFORMATION
Use web_search/web_fetch for Peter Xing's LinkedIn (https://www.linkedin.com/in/peter-xing/), Singularity University profile, speaker pages, YouTube and podcast appearances. You may review the existing public signals.json for talks Peter has shared. LinkedIn may block logged-out deep scraping; use search results and primary talk pages rather than blocking the run.

RECENCY IS HARD: every talk/appearance shown should have been published within the rolling last 12 months (today minus 365 days). For every candidate establish a real publication/recording date: use YouTube uploadDate/datePublished metadata or the article/podcast's primary published date. Discard unverifiable candidates and anything outside the window. Exponential Africa / Singularity South Africa is a useful source for recent keynotes. Safety valve only if fewer than three talks exist in-window: keep the next-most-recent verified real talks to reach three and report that the window was widened.

2) UPDATE AUTHOR DATA ONLY WHEN WARRANTED
Update C:\Users\peterxing\pap-deploy\author.json, preserving its schema: {updated,note,name,headline,linkedin,bio[],roles[{org,detail}],talks[{title,venue,year,url,blurb}]}.
- Keep name/headline/bio/roles stable. Peter's bio is his own authoritative LinkedIn About copy: preserve it verbatim and keep its paragraph count. Change role/title only on verified primary evidence, with the smallest factual edit.
- Rebuild talks[] from verified real talks, newest first, with working URLs and honest venue/year/blurb. Remove entries that aged out. Set updated to today's yyyy-MM-dd only after a successful verified refresh.
- Keep strings HTML-safe because the UI injects these fields; author.json carries no styles or markup.
- Mirror the same talk list into only the existing `<div class="grid grid-2" id="authorTalks">` inline fallback in index.html using the established author-talk card structure. Make a targeted edit; do not rewrite unrelated frontend sections.
- Validate author.json parses before continuing.

3) COMPLETE EVIDENCE-ACCOUNTING PRE-DEPLOY GUARD — MANDATORY
X EVIDENCE IS RETIRED IN FULL at the site owner's instruction. There is no Peter tier, no repost tier and no external-X tier, and the former Peter/authored floors were REMOVED from evidence-floors.json by reviewed manual edit — removed rather than zeroed, because a floor of 0 reads as a SATISFIED gate. Never reinstate one: refresh-signals.js, verify-direct-coverage.js and verify-news-evidence.js all refuse to run if a retired X floor reappears in that file.
This author job does not rematch prediction evidence and must not alter signals.json, evidence-floors.json, predictions.json, news-evidence.js, currency-evidence.js or currency-text-pins.json. NEVER edit evidence-floors.json down to make a check pass. Read the live registrations from evidence-floors.json rather than hardcoding them: currencyMaxAgeDays (14), currencyLedgerSources and currencyLedgerIdentities. Every prediction must be ACCOUNTED FOR — cited, dated CONTEXT background, or explicitly uncited, exactly one of the three, with zero in more than one and zero in none — against the current dynamic prediction ID set (103 as of 2026-08-13; always compute N, never hardcode it), with zero search fallbacks. If any of these are violated, STOP and do not deploy.

DERIVE THE GATE LIST; DO NOT REMEMBER IT. Before copying ANY file to pap-site or deploying, run every gate package.json defines — `validate`, `verify`, and every `verify:*` — from C:\Users\peterxing\pap-deploy. A HARDCODED LIST GOES STALE SILENTLY AND THAT IS EXACTLY HOW A GUARD STOPS GUARDING: this contract previously named eight gates by hand, and when `verify:browse` was added on 2026-08-18 this fail-closed deployment guard simply never ran it — no error, no warning, just an assertion that quietly stopped being checked before every deploy. The inverse failure is also real and is why the old list carried a warning: `npm run` exits 1 on a script that does not exist, so naming a removed gate (verify:archive, verify:peter, verify:external, all deleted with the X pipeline) aborts the guard before it can guard anything. Deriving from package.json fixes both directions at once. Use the helper that already does this rather than reimplementing it:
  powershell -ExecutionPolicy Bypass -File C:\Users\peterxing\pap-deploy\run-gates.ps1
If a gate must be skipped for a stated reason, name it and its reason in the report; never skip one silently and never delete one to make a run pass.

Require all PASS. The checks are dynamic and must establish:
- every current dated prediction and every post-superintelligence item is accounted for in exactly one channel: it carries a live-verified news citation, or it is explicitly recorded in signals.uncited with a reason, the search statement, searchedAt and windowDays;
- cited + uncited equals the dynamic N exactly, with zero ids double-counted and zero ids missing. app.js's hasCompleteSignalCoverage() enforces this partition at render time and blanks BOTH channels if it fails, so a defect here publishes an empty page rather than a misleading one — a green local gate with a blank live page is exactly the failure this assertion exists to catch;
- zero prediction search fallbacks;
- every citation carries a resolved article URL, publisher, byline, publication date and an exact verbatim quote, plus publishedAtSource naming whether that date came from the page or the feed. publishedAt names two different facts and they routinely disagree on a genuine article, so an undeclared disagreement fails closed. Never change a date to resolve one; declare its provenance;
- every citation still resolves: HTTP 200 on the resolved URL after redirects, unchanged headline/publisher/publication date, and its exact reviewed verbatim quote still present in the fetched article. THIS JOB VERIFIES EVIDENCE, IT NEVER CREATES OR PROMOTES IT. Never add, alter, re-point or re-review a citation from this job, and never move a prediction between the cited and uncited channels. If a citation fails to resolve or its quote has drifted, STOP and do not deploy;
- every cited article is inside the registered 14-day window, and the published payload contains ZERO X vocabulary — no x.com/twitter.com/t.co URL, no status ID, no authored/reposted tally, no oEmbed or syndication reference;
- every CURRENCY reference still resolves and still says what it said: HTTP 200 on the resolved URL, unchanged page-derived headline, publisher and date, and its exact reviewed verbatim quote still present. THIS JOB VERIFIES CURRENCY, IT NEVER CREATES, REPLACES OR RE-REVIEWS IT — that belongs to the predictions workflow. The currency layer is ADDITIVE: every prediction carrying a currency card must still carry its origin citation. verify:currency exit 75 is INFRASTRUCTURE, not an evidence fault — it means a publisher was unreachable or served a bot challenge after retries, so treat it exactly like an interlock DEFERRAL: publish nothing, change nothing, demote nothing, report it distinctly, and let the next run retry. Exit 70 is PASSED BUT INERT, a legitimate publishing state in which named axes verified NOTHING; report it as inert rather than absorbing it into a green. Never drop a citation, never relax the verbatim or drift check, and never treat HTTP 200 alone as proof a page was fetched;
- evidence is never rendered or described as an X post: citations use kind/evidenceOwner 'news' with a "News evidence — <Publisher>, <date>" label, currency cards keep the ORIGIN versus CURRENT distinction explicit, uncited predictions render their honest no-qualifying-source state rather than an empty or implied-evidence card, and the on-page provenance stamp plus signals.note stay literally true;
- every dated prediction still renders its estimated timing with wording matching its mPrecision — a named month only at "month" precision, otherwise Qn / Hn / Early-Mid-Late, never a bare year — with its ± band, and all 7 horizon items still state explicitly that they are deliberately undated. This job must never write, alter or remove an m/mBand/mPrecision/mBasis field, and must never move a month into a prediction's text;
- sourceFresh=true, a safe sourceStatus reason/action is present when a source is degraded, and there are no missing/extra prediction IDs;
- reuse occurs only in reviewed compatible concept/scenario groups and never exceeds the recorded ceiling for one source;
- the tree interlock is intact: every guarded entry point still claims the lock before reading protected files, a second live actor defers with exit 75, stale/dead-PID/corrupt locks are reclaimed, a crashed holder cannot wedge the pipeline, and .pipeline.lock is refused by the server, the deploy helper and the publisher;
- A TEXT-BASED CHECK CANNOT SEE A SYNTAX ERROR: verify:surface and verify:interlock read publish-github.ps1 as TEXT and both return exit 0 on a file that does not parse. After ANY .ps1 edit, parse it explicitly with [System.Management.Automation.Language.Parser]::ParseFile before relying on it. Exit 0 from a reader is not exit 0 from a runner;
- any unreachable, redirected-away or altered cited article fails the run closed rather than shipping a dead or misattributed citation.

If ANY coverage, provenance, resolution, freshness, interlock or prediction verifier fails, STOP before copying/deploying/GitHub. Preserve the currently published complete site, release the interlock, and report the exact failing check. Never substitute search links, weaken a gate, delete a prediction, lower a ratcheted floor, or overwrite complete production with incomplete signals. The deployment helper's own preflight must remain enabled as defense in depth.

4) SERVE AND VERIFY LOCALLY
Confirm http://127.0.0.1:8787 responds; otherwise run:
  powershell -File C:\Users\peterxing\pap-deploy\launch.ps1
The derived gate run above already covers the live browser gates (verify:author, verify, verify:ui, verify:performance) because it enumerates every `verify:*` package.json defines. Re-run it here after the server is up if the earlier pass ran without one, and require PASS. Do not maintain a second hand-written list at this step: two lists drift apart, and the one that drifts is the one nobody notices.
These must cover both themes, responsive/high-zoom/reduced-motion behavior as implemented, filters/search/deep links, the performance budget, zero console errors and no site regression.
verify:ui additionally asserts that the probability simulator's branch map MOVES when an assumption changes — measured in drawn SVG geometry, not in the text beside it. A reader reported on 2026-08-23 that changing the assumptions changed only the percentage callout; the map encoded probability solely as stroke width and opacity, so a full-slider move shifted a branch by 0.62px and left one branch identical. Every simulator assertion at the time passed, because all of them read text. If that gate fails, fix the drawing; never widen its threshold.

5) PUBLISH TO VERCEL
Only after all author, forecast, direct-evidence, external-resolution, UI and performance checks pass, sync the approved explicit public files from pap-deploy to pap-site using ONLY C:\Users\peterxing\pap-site\deploy.ps1. The site is split across multiple runtime files, so index.html, app.js and styles.css must be synced together with author.json, signals.json and predictions.json — a partial copy will ship a broken or stale site. Never hand-copy, never use wildcard copies, and never stage anything outside the explicit public allow-list or from pap-secrets. The hardened helper copies and SHA-256 verifies the six runtime files; any mismatch must fail the run.

Clear VERCEL_TOKEN and use cached Vercel authentication, then run:
  powershell -ExecutionPolicy Bypass -File C:\Users\peterxing\pap-site\deploy.ps1
Require exit 0, the deploy preflights to pass, and Vercel Ready/Deploy complete; helper exit 75 is the currency INFRASTRUCTURE deferral described above and means stop without publishing, not fail. Verify live author content and the complete evidence accounting on BOTH https://peterxing.com and https://post-agi-planning.vercel.app. Re-run the relevant author, site, prediction/direct-coverage and UI verifiers against production; require both themes, every prediction accounted for in exactly one channel, zero prediction searches and zero console errors.

INDEPENDENT LIVE POST-DEPLOY ASSERTION — do not trust local state or the deploy helper's exit code alone. Fetch https://peterxing.com/signals.json and https://post-agi-planning.vercel.app/signals.json directly with a cache-busting query string and assert against the FETCHED PRODUCTION JSON (never the local file) that the cited embed ids plus the uncited ids equal the dynamic N from predictions.json, with zero ids in both channels and zero in neither; the search object is empty; grouping embeds by evidenceOwner yields news only with zero X evidence of any kind; every citation declares publishedAtSource and falls inside the registered 14-day window; unique source count and maximum per-source reuse satisfy the recorded ceiling; every currency link in coverage.currency corresponds to a prediction that still has its origin citation, proving the layer is still additive rather than substitutive; and sourceFresh is true. Assert ZERO occurrences of x.com, twitter.com, t.co or any other retired X vocabulary in the fetched signals.json, app.js and index.html. Both domains must report identical composition. Because a green local gate can coexist with a blank live page, also re-fetch at least 3 cited article URLs taken from the LIVE JSON and confirm HTTP 200 on the final URL with the recorded verbatim quote still present. Also confirm production refuses /.pipeline.lock. Any failed assertion is a live regression: report it immediately and do not describe the run as successful. Finally run `node verify-deploy-surface.js --live` and require PASS: it probes the full repository inventory against both domains and fails if ANY non-public path — a script, corpus, cache, ledger or lock file — is reachable, or if any approved public file stopped being served. This guard verifies the surface but must never widen it: it may not add a re-include to .vercelignore, and it may not publish a file that is not already on the approved public surface.

6) MIRROR TO GITHUB
After Vercel succeeds, run only:
  powershell -ExecutionPolicy Bypass -File C:\Users\peterxing\pap-deploy\publish-github.ps1
Do not hand-roll git. The helper uses an explicit secret-free allow-list, runs its own coverage/news/currency/interlock/surface preflight, and aborts on forbidden files including .pipeline.lock. Require exit 0; normal git stderr banners are not errors. A mirror failure does not undo a successful Vercel deploy, but report it plainly. Never paste or commit credentials. Then release the interlock:
  node pipeline-lock.js release --owner=$env:PAP_PIPELINE_OWNER

7) REPORT
Report: whether this was a normal or CATCH-UP run and the state of the previous run; whether the interlock was acquired, inherited, reclaimed from a stale holder, or DEFERRED, and confirmation that it was released; any transient infrastructure retries and whether they succeeded; talks added/updated/removed with verified source and date; explicit last-12-month compliance or safety-valve use; author.json updated date; whether bio/roles changed; local and live author/site/UI/performance results; the evidence accounting — cited + uncited against the dynamic N, stated with zero double-counted and zero missing rather than implied by a single total; zero-search status; the independent live post-deploy assertion results for BOTH domains including the sampled citation re-fetch and the zero-X-vocabulary check; Vercel production URLs; and GitHub commit/no-change/failure. Keep reporting concise and never expose private activity, private corpus records or credentials.

DAILY ENFORCEMENT IS AN ASSUMPTION PETER CAN OVERRULE. Verifying the currency layer and the estimated-month rendering on every run is treated as a mandatory daily obligation of this guard because that is the most defensible reading of the standing instruction. If Peter wants a different cadence, say so in the report and follow his direction rather than silently continuing to enforce one he has overruled.